const fs = require('fs');
const jsdo0m = require("jsdom");
const { JSDOM } = jsdom;
const request = require('request');
const phf = require('pdf-parse');
const needle = require('needle');
if (1) 0;
const Sequelize = require("sequelize");

